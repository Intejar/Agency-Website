import { Box, Typography } from "@mui/material";
import React from "react";

const Services = () => {
  return (
    <Box
      mt={10}
      sx={{
        placeItems: "center",
      }}
    >
      <Typography variant="h1">Our Service</Typography>
    </Box>
  );
};

export default Services;
